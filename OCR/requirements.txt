# DeepSeek-OCR Model and Transformer Ecosystem
torch>=2.0.0
transformers>=4.40.2
accelerate>=0.28.0
pillow>=10.3.0 # For image handling (PIL)
sentencepiece>=0.1.99 # Often a dependency for DeepSeek-OCR tokenizer

# PDF Handling (Option 1: Recommended for robustness and speed)
PyMuPDF>=1.24.1 # Used for converting PDF pages to high-resolution images

# Alternative PDF Handling (Option 2: Requires Poppler dependency)
# pdf2image>=1.16.0 

# Other general dependencies
tqdm>=4.66.2 # Often used for progress bars (e.g., in Hugging Face)

# FastAPI and dependencies for serving the API
fastapi>=0.111.0
uvicorn[standard]>=0.30.1 # ASGI server for running FastAPI
python-multipart>=0.0.9   # Required for handling file uploads (multipart/form-data)


addict>=2.4.0
matplotlib>=3.8.4
torchvision>=0.17.2 # Must match your Torch version compatibility